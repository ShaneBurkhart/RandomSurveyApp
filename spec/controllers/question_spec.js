'use strict'

var QuestionController = require('../../controllers/question.js');

describe('QuestionController', function() {
  it('shows a question', function() {
    expect(false).toBe(true);
  });

  it('renders a message when no questions are left', function() {
    expect(false).toBe(true);
  });

  it('doesn\'t show the same question twice', function() {
    expect(false).toBe(true);
  });
});
